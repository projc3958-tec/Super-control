{
  "name": "clipboard-sync-server",
  "version": "1.0.0",
  "description": "WebSocket relay server for clipboard sync + remote window control",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "ws": "^8.16.0"
  },
  "engines": {
    "node": ">=18 <22"
  }
}
